实验四：为有向图设计存储结构(建议用邻接表)，并对比图（AOV-图）进行拓扑排序，以此判断图中是否含有回路

分析：

图的存储结构
	Point{
		int id: 该点的编号
		int in: 该点的入度
		list edges: 从该点连接到其他点的集合,保证唯一
	}

输入：
	第一行：m个结点(编号1~m)，n条边
	第2~n+1行，两个数字分别为a,b;代表a——>b,既b的前驱是a
	注意：若输入数据有误,例如输入a=1,b=1 或者a=-1,b=m-1,自动忽略
输出：
	进行拓扑排序，以此判断图中是否含有回路


	 

